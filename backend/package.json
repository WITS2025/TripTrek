{
  "name": "triptrek-backend",
  "private": true,
  "scripts": {
    "install-all": "npm --prefix create install && npm --prefix read install && npm --prefix update install && npm --prefix delete install"
  }
}
